{
    "apiVersion": "v1",
    "kind": "Service",
    "metadata": {
        "name": "orderable-svc",
        "labels": {
            "app": "orderable-api",
            "env": "{{NAMESPACE}}"
        }
    },
    "spec": {
        "ports": [
            {
                "port": 443,
                "targetPort": 443
            }
        ],
        "selector": {
            "app": "orderable",
            "env": "{{NAMESPACE}}"
        },
        "type": "NodePort"
    }
}
